
export enum UserStatus {
  ACTIVE = 'active',
  PENDING = 'pending',
  DELETED = 'deleted',
  BLOCKED = 'blocked'
}

export enum JwtTokenType {
  AcessToken = 'acessToken',
  RefreshToken = 'refreshToken'
}